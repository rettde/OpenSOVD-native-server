apiVersion: v1
kind: Service
metadata:
  name: {{ include "opensovd.fullname" . }}
  labels:
    {{- include "opensovd.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    {{- if .Values.tls.enabled }}
    - port: {{ .Values.tls.port }}
      targetPort: https
      protocol: TCP
      name: https
    {{- end }}
  selector:
    {{- include "opensovd.selectorLabels" . | nindent 4 }}
